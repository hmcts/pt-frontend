import { formatCaseReferenceForDisplay } from '@utils/caseReference';

export const caseReferenceDisplay = (caseReference: string): string => {
  return formatCaseReferenceForDisplay(caseReference);
};
