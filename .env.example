# Local secrets needed for app to run, copy to .env and fill in 
PT_FRONTEND_IDAM_SECRET=

# Serve over plain HTTP locally: drops the session cookie's `Secure` attribute and
# builds an http:// OIDC redirect URI. Never set this in a deployed environment.
ALLOW_INSECURE_COOKIES=true
