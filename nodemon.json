{
  "watch": ["src/main"],
  "ext": "ts,js",
  "ignore": ["src/**/*.spec.ts"],
  "exec": "./bin/generate-ssl-options.sh && node --env-file-if-exists=.env -r ts-node/register -r tsconfig-paths/register ./src/main/server.ts",
  "env": {
    "NODE_ENV": "development"
  }
}
